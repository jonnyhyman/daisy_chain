import { resolve } from "daisychain";

console.log(resolve);

describe('get_resolve_object', () => {
  test('test_connection', () => {
    // Setup test, if necessary
    // Assert the expected outcome
    expect(result).toBe(true);
  });
});
